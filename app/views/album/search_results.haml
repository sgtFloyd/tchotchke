- if albums && albums.length > 0
  - albums.first(10).each do |album|
    .result{ 'data-artist' => album.artist, 'data-album' => album.name }
      ="~"
      %a.album_result{href:'javascript:'}= album.name
- else
  %br
  %i= "No albums found."