- if tracks && tracks.count > 0
  - tracks.each do |track|
    .result{ 'data-artist' => track.artist, 'data-album' => track.album, 'data-track' => track.name }
      ="~"
      %a.track_result{href:'javascript:'}= track.name
- else
  %br
  %i= "No tracks found."